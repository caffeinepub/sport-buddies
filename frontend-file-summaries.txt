{
  "frontend/src/App.tsx": "TanStack Router configuration updated to use `/events/$eventId` (renamed from `$id`) for the event detail route, with all other routes and layouts unchanged.",
  "frontend/src/components/Header.tsx": "Fixed header with maroon background showing Sport Buddies SB badge and wordmark on the left (clicks → `/`), coin balance pill as a clickable button (→ `/coins`), UPC/QR scan button (→ `/scan`), and Emergency shield button (→ `/emergency`) on the right, using existing brand colors.",
  "frontend/src/components/BottomTabBar.tsx": "Bottom navigation bar with five tabs (Home→`/`, Map→`/map`, Events→`/events`, Coins→`/coins`, Profile→`/profile`) using maroon-dark background and gold active state; Home tab uses exact-match active detection to avoid false positives on sub-routes.",
  "frontend/src/pages/HomePage.tsx": "HomeScreen that now imports `useSport` from SportContext and uses `sportStatus`, `currentSport`, and `deactivateSport` to make the primary CTA button dynamic — showing \"Activate My Sport\" when inactive and \"Deactivate {sport}\" when active, calling `deactivateSport()` or navigating to `/activate` accordingly, with a gold \"🔥 Out Now • {sport}\" badge rendered below the button only when a sport is active.",
  "frontend/src/pages/MapPage.tsx": "Map page that now imports `useSport` from SportContext and uses a `useEffect` to auto-sync `selectedFilter` with `currentSport` (lowercased) when `sportStatus === \"active\"`, resetting to `\"all\"` when inactive; renders `filteredBuddies` derived from the filter state instead of the raw buddies array.",
  "frontend/src/pages/EventsPage.tsx": "Events list page that now imports `useEffect` and consumes `sportStatus`/`currentSport` from SportContext via `useSport`, auto-syncing `selectedSport` to the active sport when `sportStatus === \"active\"` and resetting to \"All\" when inactive; renders `filteredEvents` derived from the filter state with manual chip selection preserved.",
  "frontend/src/pages/SessionPage.tsx": "Placeholder session page component displaying \"Session\" heading and dynamic session ID from TanStack Router route parameters using the correct 'sessionId' parameter name.",
  "frontend/src/pages/CoinsPage.tsx": "Coins page with ScreenBanner (\"CoinsScreen\" | \"Coins\") as the first rendered element, balance card, and four action cards (Helicopter, Scan UPC, Scan QR, Invite Friends); restructured to place banner outside the padded inner container.",
  "frontend/src/pages/StorePage.tsx": "Store page with product listing, category filters, and redemption flow; fixes `addRedemption(name, cost)` call signature and correctly extracts `redemption.id` (string) for navigation.",
  "frontend/src/pages/MarketplacePage.tsx": "Marketplace page with two tabs (Gear Market and Pro Shops); all item cards have wired \"View Details\" and \"Contact Seller\" buttons showing toast feedback, \"Post an Item\" button shows coming-soon toast, and \"View Shop\" buttons show location info toasts.",
  "frontend/src/pages/ProfilePage.tsx": "Profile page with ScreenBanner (\"ProfileScreen\" | \"Profile\") as the first rendered element, followed by profile header, sport status, status selector, location toggle, emergency shield button, and sign out button; restructured to place banner outside the padded inner container.",
  "frontend/src/pages/EmergencyPage.tsx": "Placeholder emergency page component displaying \"Emergency\" heading with proper spacing for header only (no bottom tab bar).",
  "frontend/src/pages/HelicopterPage.tsx": "Complete helicopter reservation page with date/time picker, real-time availability checking against Google Calendar, reservation form with validation (name, email, phone, passenger count 1-2, weight, notes), dual payment options (2000 coins or paid), coin balance validation, race condition handling, success confirmation screen, and graceful error handling for calendar configuration issues.",
  "frontend/src/pages/AuthPage.tsx": "Authentication page with both Sign In and Sign Up buttons using Internet Identity, includes post-login profile completion check and automatic routing to /profile-setup or '/' (home), with corrected route path.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend operations including useCompleteProfile mutation, useIsProfileComplete query, and new useGetUserProfile query that fetches user profile data from backend with proper error handling for users without profiles.",
  "frontend/src/index.css": "Global CSS with Sport Buddies maroon/gold/black brand theme using OKLCH color system, black-to-charcoal gradient background, white/muted text colors, CSS variables for all brand tokens, and utility classes for primary/secondary buttons and form inputs with gold focus states.",
  "frontend/src/pages/ProfileSetupPage.tsx": "Profile setup page with charcoal card background, form inputs styled with subtle backgrounds and gold focus borders, muted text labels, white value text, and gold primary submit button.",
  "frontend/src/components/ErrorBoundary.tsx": "React Error Boundary class component that catches render errors globally and displays a fallback screen with Sport Buddies branding, error message, and a Go Home button.",
  "frontend/src/components/DebugBanner.tsx": "Debug banner component that displays current route, authentication status, and profile completion status as small pill-style badges below the header on all pages.",
  "frontend/src/components/LoadingFallback.tsx": "Loading fallback component that displays \"Loading Sport Buddies…\" text with an animated spinner to prevent blank screens during loading states.",
  "frontend/src/pages/NotFoundPage.tsx": "Not found page component that displays \"Route Not Found\" message with a Go Home button navigating to '/' for unmatched routes.",
  "frontend/src/main.tsx": "Application entry point wrapping the app with ErrorBoundary, QueryClientProvider, and InternetIdentityProvider to ensure proper error handling and state management.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with Sport Buddies brand color tokens (maroon, maroon-dark, gold, gold-dark, charcoal, black) mapped to CSS variables, gold shadow utilities including gold-glow for hover effects, and proper OKLCH color system integration.",
  "frontend/src/hooks/useActivationStatus.ts": "Custom hook managing sport activation state with localStorage persistence, providing activate function, status tracking, 10-second countdown timer, and automatic cleanup when expiry is reached.",
  "frontend/src/pages/SafetyPage.tsx": "Safety page with two badge cards (Emergency Shield and Emergency Notifications); SETUP buttons navigate to /emergency or show a toast, LEARN buttons show informational toast messages — no dead buttons.",
  "frontend/src/hooks/useCoinBalance.ts": "Shared hook managing coin balance in localStorage under sb_coinBalance key with cross-component synchronization via coinBalanceUpdated custom events, providing balance state and update functions with safe error handling.",
  "frontend/src/hooks/useCoinScanner.ts": "Custom hook integrating QR scanner with coin validation logic, tracking claimed codes in localStorage, processing scans against hard-coded valid codes (HELI-50, FERRARI-50, SWAG-20), and awarding coins for first-time valid scans.",
  "frontend/src/pages/ScannerPage.tsx": "Preview-safe scanner stub at /scan showing \"Scan UPC / QR\" heading, body text, a camera placeholder, and \"Unavailable in preview. Use on device build.\" message — no camera permissions requested.",
  "frontend/src/pages/EmergencyShieldPage.tsx": "Emergency Shield page with press-and-hold activation (2s), confirmation modal, status display (SAFE/ACTIVE), action buttons (notify buddies, emergency contact, call 911, end emergency), 60-second cooldown with countdown, and localStorage logging of all emergency events and actions.",
  "frontend/src/pages/SOSLogPage.tsx": "SOS log page displaying emergency events from localStorage in reverse chronological order with start/end times, duration, and actions taken, includes Back button and Clear Log button with confirmation modal that removes all logs from localStorage.",
  "frontend/src/services/calendarIntegration.ts": "Calendar integration service stub module with getCalendarFreeBusy and createBookingEvent functions returning mocked values, structured for future Google Calendar API integration with detailed JSDoc comments indicating Google Calendar as source of truth for pilot availability and bookings.",
  "frontend/src/hooks/usePresenceState.ts": "Custom hook managing presence state in localStorage with five keys (presenceStatus, presenceSport, presenceActivatedAt, presenceExpiresAt, pocketFlashUntil), providing functions to activate presence with 10-minute expiry, clear presence, check expiry automatically on mount and every 30 seconds, and return current presence state including flash status.",
  "frontend/src/hooks/useLocationPermission.ts": "Custom hook for checking and requesting browser geolocation permission using navigator.geolocation API, providing permission state ('granted', 'denied', 'prompt', 'unsupported'), request function, and checking state with fallback for browsers without Permissions API.",
  "frontend/src/components/LocationPermissionModal.tsx": "Modal component using shadcn/ui Dialog displaying \"Location required to activate presence.\" message with two action buttons ('Enable Location' triggers permission request, 'Cancel' dismisses modal) styled with Sport Buddies maroon/gold theme.",
  "frontend/src/components/PresenceBanner.tsx": "Full-width fixed banner component displaying \"LIVE: {sport} — Out Now\" with pulsing dot indicator that animates for exactly 10 seconds based on pocketFlashUntil timestamp, then remains visible without animation using Sport Buddies primary/accent colors.",
  "frontend/src/pages/PresenceDetailPage.tsx": "Presence detail page showing buddy info, public billboard, and action buttons (Invite to Event → /events, Scan Buddy QR → /scan); uses `useParams({ strict: false })` to avoid route-id mismatch.",
  "frontend/src/pages/CreateEventPage.tsx": "Create event stub page that reads the sport query param via `window.location.search` instead of `useSearch({ from })` to avoid route-id mismatch errors with nested layout routes.",
  "frontend/src/hooks/useRedemptions.ts": "Custom hook managing redemption records in localStorage with functions to add redemptions (generating unique IDs and SB-XXXX-XXXX codes), retrieve all redemptions, and get single redemption by ID with cross-tab synchronization.",
  "frontend/src/pages/RedemptionDetailPage.tsx": "Redemption detail page showing item info, redemption code with copy button, and claim instructions; uses `useParams({ strict: false })` to avoid route-id mismatch.",
  "frontend/src/pages/EventDetailPage.tsx": "Full Event Details screen reading event data from SportContext via the `eventId` route param; renders header (title, sport badge, datetime, location, host, status chip), participants list with count/capacity, Join/Leave buttons (hidden when event is not active), host-only Complete/Cancel controls, Attendance Validation card with \"Mark Attended (MVP) +5 coins\" / \"Attended ✅\" button, and a rules footer.",
  "frontend/src/hooks/useEventJoinState.ts": "Custom React hook managing event join/leave state with localStorage persistence using sb_joinedEventIds and sb_eventParticipantOverrides keys, providing methods to check join status, get participant counts with overrides, join/leave events, and cross-tab synchronization with safe error handling.",
  "frontend/src/hooks/useAttendance.ts": "Custom hook managing attendance state in localStorage with sb_attendedEventIds key, providing methods to check if event is attended, mark event attended (adding +5 coins via useCoinBalance integration), and retrieve attended event IDs with cross-tab synchronization and safe error handling.",
  "frontend/src/pages/CoinGrabPage.tsx": "CoinGrab Scanner page with real camera QR scanning using useQRScanner hook, permission explanation card before camera access, camera permission handling with clear error messages, auto-pause after successful scan with \"Scan Again\" button, validation logic for HELI-50/FERRARI-50/SWAG-20 codes, localStorage management for claimed codes, coin awarding via useCoinBalance hook, success animation overlay, and dark maroon/gold Sport Buddies theme styling.",
  "frontend/src/hooks/useHelicopterAvailability.ts": "React Query hook that calls backend.checkHelicopterAvailability to fetch busy time blocks from Google Calendar, parses JSON response, handles calendar configuration errors gracefully, and returns availability data with error states.",
  "frontend/src/hooks/useHelicopterBooking.ts": "React Query mutation hook that calls backend.bookHelicopter with reservation details, parses JSON response to extract success/failure status, handles race conditions when slots become unavailable, and invalidates availability queries after successful bookings.",
  "frontend/src/pages/InvitePage.tsx": "Invite Friends page with a copyable invite link, clipboard copy with toast feedback, native share API support, back navigation to home, and a \"how it works\" explanation card.",
  "frontend/src/pages/HowItWorksPage.tsx": "How Coins Work page with four numbered step cards explaining sport activation, QR scanning, event attendance, and store redemption, plus a coin values table and a back-to-home button.",
  "frontend/src/pages/ScanPage.tsx": "Scan page at /scan with a \"Scan\" heading, two action buttons (\"Scan UPC / CoinGrab\" and \"Scan QR Badge\") that check camera availability before navigating to /coin-grab, and show a preview-safe toast fallback message when camera is unavailable.",
  "frontend/src/pages/ActivateSportScreen.tsx": "Glove Mode / One-Touch Activation screen at `/activate` that now derives `isActive` from `sportStatus === \"active\"` via SportContext, renders a single smart button showing `\"Deactivate {currentSport}\"` when active or `\"Activate My Sport\"` when inactive, and calls `deactivateSport()` or `handleActivate()` accordingly; sport chips are disabled while a sport is active to prevent mid-session switching.",
  "frontend/src/utils/safeNavigate.ts": "Utility function that checks a target path against a whitelist of known valid routes and either calls navigate or shows a \"Coming soon (MVP).\" toast if the route is not registered.",
  "frontend/src/pages/ScannerStubPage.tsx": "New scanner stub page at /scanner-stub that reads a 'type' query param (upc or qr) to display the appropriate scanner title, shows the \"Camera scanning requires a real device build. Preview mode shows this message only.\" message, and provides a Go Back button to /coins.",
  "frontend/src/components/ScreenBanner.tsx": "New debug locator banner component that renders a yellow (#FFD54A) bar with bold black monospace text showing \"SCREEN: {screenName} | ROUTE: {routeName}\" — used as the topmost element on every tab and stack screen for wiring verification.",
  "frontend/src/context/SportContext.tsx": "Extended global sport context that now includes `currentUser` (id='me'), a full `events` array with 5 mock events (each with id, hostId, hostName, participants[], status, capacity), event management actions (joinEvent, leaveEvent, completeEvent, cancelEvent), and attendance tracking (attendedEventIds, hasAttended, markAttended) with localStorage persistence and +5 coin rewards; all existing state (sportStatus, currentSport, userMode, locationEnabled) is preserved."
}